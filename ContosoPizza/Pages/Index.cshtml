<!DOCTYPE html>
<html>
<head>
	<title>Thông báo</title>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<style>
		.popup {
			position: fixed;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background-color: rgba(0, 0, 0, 0.5);
			display: flex;
			justify-content: center;
			align-items: center;
		}

		.popup-message {
			background-color: #fff;
			padding: 20px;
			border-radius: 5px;
			box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
		}

			.popup-message h2 {
				margin-top: 0;
			}

			.popup-message p {
				margin-bottom: 0;
			}

		.popup-close {
			position: absolute;
			top: 10px;
			right: 10px;
			cursor: pointer;
		}
	</style>
</head>
<body>
	<div class="popup">
		<div class="popup-message">
			<h2>Thông báo</h2>
			<p>Không thể tạo đơn hàng với coupon đã sử dụng.</p>
			<p>Vui lòng thử lại với coupon khác hoặc không sử dụng coupon.</p>
			<div class="popup-close">Đóng</div>
		</div>
	</div>

	<script>
		// Lấy đối tượng popup và nút đóng
		var popup = document.querySelector('.popup');
		var closeButton = document.querySelector('.popup-close');

		// Sự kiện click cho nút đóng
		closeButton.addEventListener('click', function () {
			popup.style.display = 'none';
		});

		// Hiển thị popup
		popup.style.display = 'flex';
	</script>
</body>
</html>