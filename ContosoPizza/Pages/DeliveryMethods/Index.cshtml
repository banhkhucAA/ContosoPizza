@page
@model ContosoPizza.Pages.DeliveryMethods.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">Delivery Methods</h1>

<p class="text-center">
    <a class="btn btn-primary" asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryMethod[0].Method)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryMethod[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DeliveryMethod) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Method)
            </td>
            <td>
                $@Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                    <div>
                        <a class="btn btn-primary btn-icon-split" asp-page="./Edit" asp-route-id="@item.Id">
                            <span class="icon text-white-50">
                                <i class="fas fa-flag"></i>
                            </span>
                            <span class="text min-width-button-CRUD">Edit</span>
                        </a>
                    </div>
                    <div>
                        <a class="btn btn-info btn-icon-split" asp-page="./Details" asp-route-id="@item.Id">
                            <span class="icon text-white-50">
                                <i class="fas fa-info-circle"></i>
                            </span>
                            <span class="text min-width-button-CRUD">Details</span>
                        </a>
                    </div>
                    <div>
                        <a class="btn btn-danger btn-icon-split" asp-page="./Delete" asp-route-id="@item.Id">
                            <span class="icon text-white-50">
                                <i class="fas fa-trash"></i>
                            </span>
                            <span class="text min-width-button-CRUD">Delete</span>
                        </a>
                    </div>
            </td>
        </tr>
}
    </tbody>
</table>
